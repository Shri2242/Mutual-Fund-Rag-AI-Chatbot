# ─── Minimal requirements for Streamlit deployment ──────────────────────────
# No chromadb, sentence-transformers, torch, or playwright (saves ~2GB)

streamlit>=1.57.0          # Web app framework
httpx>=0.28.0              # HTTP client for Groq API calls
python-dotenv>=1.0.0       # Load .env for API keys
